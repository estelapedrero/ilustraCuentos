<div class="row fullPage">
	<h1 class="col-xs-8 col-xs-offset-2"><%= @book.title %></h1>
</div>
<div class="row">
	<%= icon('chevron-circle-left', '', class: 'col-xs-1') %>
	<div class="col-xs-10 ">
		<% @book.pages.each do |p| %>
			<p class="textoCuento"><%= p.content %></p>
		<% end %>
	</div>
	<%= icon('chevron-circle-right', '', class: 'col-xs-1') %>
</div>
